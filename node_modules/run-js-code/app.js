const fs = require('fs');


const writeFile = (code, funcName) => {

    fs.writeFileSync('./testFuncs.js', `${code} 
module.exports = {func: ${funcName} };`);
    
    const {func} = require('./testFuncs');

    return func;
}

module.exports = { writeFile };